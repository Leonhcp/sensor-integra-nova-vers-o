// const {PubSocket} = require("pubsocket")

// const pubSocket = new PubSocket()

// const channel = pubSocket.createChannel('ROOM')

// channel.addListener((data) => {console.log(data)})

// channel.publish("Server Message: oi.")

// setTimeout(() => {
//     channel.disconnect();
  
//     pubSocket.close();
//   }, 10000)